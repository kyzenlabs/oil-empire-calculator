[build]
  publish   = "public"
  functions = "netlify/functions"
  command   = "echo 'no build step'"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from   = "/api/*"
  to     = "/.netlify/functions/:splat"
  status = 200
